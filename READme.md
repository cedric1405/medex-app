# 💊 MedEx - Plateforme de vitrine pharmaceutique

## 🧩 Structure

- `backend/` : Projet Django (API, gestion des données)
- `frontend/` : Application React (interface utilisateur)

## 🚀 Installation

### Backend (Django)

```bash
cd backend
python -m venv env
source env/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

##frontend
cd YMGS-Frontend
npm install
npm run dev
```
